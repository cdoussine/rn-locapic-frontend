export default function(id = {}, action) {

    if(action.type == 'signin') {
        // console.log('Dans mon reducer --->',action.id)
        return action;
    } else {
        return action;
    }
  }